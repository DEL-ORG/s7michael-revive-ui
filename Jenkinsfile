pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                git url: 'git@github.com:DEL-ORG/s7michael-revive-ui.git', branch: 'main'
            }
        }
        // Other stages...
    }
}
